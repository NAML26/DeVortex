    // Código JavaScript para enviar los datos del formulario al API
    // Seleccionar el formulario y el elemento donde se mostrará el mensaje
   // const formulario = document.getElementsByName("form");
    // const mensaje = document.getElementById("mensaje");

    // Agregar un evento de submit al formulario
   // formulario.addEventListener("submit", function(evento) {
      // Evitar que el formulario se envíe de forma predeterminada
     // evento.preventDefault();
      // Obtener los valores de los campos del formulario
    //  const inputCalle = form.inputCalle.value;
    //   const precio = formulario.precio.value;
    //   const stock = formulario.stock.value;
    //   const imagen = formulario.imagen.value;
      // Crear un objeto con los datos del artículo
    //  const articulo = {
        //inputCalle: inputCalle
        // precio: precio,
        // stock: stock,
        // imagen: imagen
    /*   }; */
      // Crear una petición POST al API usando la API Fetch
    /*   fetch("http://localhost:3000/articulos", {
        method: "POST",
        headers: {
          "Content-Type": "application/json"
        },
        body: JSON.stringify(articulo)
      })
      .then(function(respuesta) { */
        // Comprobar si la respuesta es exitosa
      //  if (respuesta.ok) {
          // Mostrar un mensaje de éxito
        //  mensaje.textContent = "Artículo agregado correctamente";
          // Limpiar los campos del formulario
          //formulario.reset();
        //} else {
          // Mostrar un mensaje de error
          //mensaje.textContent = "Error al agregar el artículo";
       // }
      //})
      //.catch(function(error) {
        // Mostrar un mensaje de error
        //mensaje.textContent = "Error al agregar el artículo";
      //});
    //});